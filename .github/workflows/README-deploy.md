# Deployment docs have moved

See [`README-DEPLOY.md`](../../README-DEPLOY.md) in the repository root for
setup steps, required secrets, and known issues.

(GitHub Actions only reads `.yml` / `.yaml` files from this directory, so this
file has no effect on the workflow itself. Safe to delete.)
